import './styles.css';
import Button from 'components/Button';

const ProductCard = () => {
  return (
    <div className="base-card serch-card">
      <input type="text" />
      <Button />
    </div>
  );
};

export default ProductCard;
